// const db = require('./connection');

// const getItemsByRestaurantID = (restaurantId) => {
//   return db.query('SELECT * FROM menuitems WHERE restaurant_id = $1', [restaurantId])
//     .then((response) => {
//       return response.rows;
//     });
// };

// // const getItemsById = (id) => {
// //   return db.query('SELECT * FROM menuitems WHERE id = $1', [id])
// //     .then((response) => {
// //       return response.rows[0];
// //     });
// // };

// module.exports = {
//   getItems,
//   getItemsById
// };
